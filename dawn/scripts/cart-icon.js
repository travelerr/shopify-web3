import ReactDOM from "react-dom";
import React from "react";
import { CartIcon } from "./Components/CartIcon";

const rootEl = document.getElementById("react-icon-cart");

rootEl && ReactDOM.render(rootEl);
